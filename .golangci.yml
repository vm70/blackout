version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - funlen
    - gochecksumtype
    - goconst
    - godot
    - nilerr
    - revive
    - sloglint
formatters:
  enable:
    - gofumpt
